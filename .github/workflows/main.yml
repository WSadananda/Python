name: action cicd
on:
  push:
      branches:["master"]
jobs:
    action:
     runs-on: ubunto-latest
    
    steps:
     
        - name: Checkout code
            uses: actions/checkout@v4
        - name: run command
             run: echo "run success"
